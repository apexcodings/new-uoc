<div class="main-content-wide">
  <h1>Editing</h1>
  <h2><%= @page.title %></h2>

  <%= form_for @page do |f| %>
    <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
    </p>

    <p>
      <%= f.submit "Update Page" %>
    </p>
  <% end %>
</div>

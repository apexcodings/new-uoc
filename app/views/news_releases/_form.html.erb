<%= form_for @news_release do |f| %>
  <%= render "shared/errors", object: @news_release %>

  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :body %>
    <%= f.cktext_area :body %>
  </p>

  <p>
  <% if @news_release.new_record? %>
    <%= f.submit "Create News Release", class: "green-link" %>
  <% else %>
    <%= f.submit "Update News Release", class: "green-link" %>
  <% end %>
  </p>
<% end %>
